version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: pixelrealm-postgres
    environment:
      POSTGRES_DB: pixelrealm
      POSTGRES_USER: pixelrealm
      POSTGRES_PASSWORD: pixelrealm_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pixelrealm -d pixelrealm"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: pixelrealm-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
